# Triage Run Log

2026-09-01 — BLOCKED before Phase 1: this session's GitHub access is scoped to `itzskyeyt/intertune` only (confirmed via a `list_issues` call against `OuterTune/OuterTune`, which returned "Access denied: repository ... is not configured for this session"). No `gh` CLI, no direct GitHub API access, and no upstream remote configured either. Since issues live upstream at OuterTune/OuterTune, Phase 1 (recon/triage) could not run — 0 issues scanned, 0 scored. Build-capability probe still ran (independent of GitHub access): no Android SDK present (`$ANDROID_HOME`/`$ANDROID_SDK_ROOT` unset, no `local.properties`), and `./gradlew --offline compileDebugKotlin` fails during plugin resolution (offline mode can't resolve `com.google.dagger.hilt.android`). Recorded `build_capability: "none"`. No fix attempted, no fix possible without upstream issue access.

corrected: the above entry only tried the GitHub connector (scoped to itzskyeyt/intertune) and gave up without trying an unauthenticated route. Re-verified this run: raw `curl` to both `api.github.com` and `github.com` IS genuinely intercepted by this session's own network/egress policy and returns a synthetic access-denial message regardless of target repo (confirmed by testing an unrelated public repo, torvalds/linux, which got the identical block) — so this is a real session-level constraint, not a misdiagnosis of GitHub permissions. However, WebFetch reaches public GitHub pages (issue lists, search UI, individual issues) successfully. Recon this run used WebFetch against `github.com` search-filtered issue lists and individual issue pages instead of the JSON REST API. This is far more expensive per request (AI-summarized HTML, not structured JSON) and not exhaustive over the full ~294-issue backlog — recon was narrowed via label-filtered searches (`label:bug`, `label:confirmed`, `label:CRITICAL`, plus component labels `c: Player`/`c: Library`/`c: Local media`, each combined with the standard disqualifying-label exclusions) rather than the intended 4-request full-backlog scan. Future runs: try WebFetch directly rather than raw curl/API calls against github.com in this environment; the `rate_limit` check against `api.github.com` still works (it's a generic public endpoint) but is irrelevant here since the actual issue-listing endpoints are blocked for this session regardless of repo.

2026-09-01 — api ~14983/15000 left (curl rate-limit check only; recon done via WebFetch, not counted against this budget), scanned ~30 (label-filtered subset, not full 294-issue backlog), scored 9, top #883 (conf 55/100, "downloaded song from artist page fetches online instead of locally") — no fix: build_capability=none this run caps verifiability low (max ~5/15), diagnostic evidence capped low (no logs/stack trace/viewable recording, max 3/20 for the top candidate), and the identified root cause (MusicService.kt createDataSourceFactory not falling back to a DB lookup when queue-supplied MediaMetadata omits local-download info) confidently explains only the "external/custom download" half of the report, not the "internal download" half — capping localization at 12/25. Two other candidates hit hard aborts: #787 (scrollbar flicker) root cause lives in third-party dependency `nanihadesuka/LazyColumnScrollbar`, already documented as such in `ui/component/ScrollBar.kt:18-19`; #965 (plaintext auth tokens) needs either a new Gradle dependency (forbidden) or touching ~10 call sites with no shared accessor plus a migration, past blast-radius limits. #1190 ("search null crashes") turned out to already be fixed in this fork — verified directly in MainActivity.kt and OnlineSearchViewModel.kt. Full breakdown in `.issue-triage/index.json` and `.issue-triage/issues/*.md`.

# 369 — Issue with Android Auto: queue jumps back to top when scrolling

**Labels:** bug · **State:** blocked · **Last scored:** 2026-09-02

## What breaks
On Android Auto, opening the queue and trying to scroll snaps the list back to the top, so the
queue is effectively unscrollable.

## Where
Not located. The Android Auto queue view is not drawn by the app — it is rendered by the AA host
from what the `MediaSession` publishes. The plausible source is the app re-publishing the queue
or the session timeline on every position update, which makes the host reset its scroll offset.
Nothing in the report narrows it below "the media-session/queue publishing path".

## Evidence present / missing
- **Present:** four numbered repro steps, clear expected/actual.
- **Missing:** logs (`No response`), screenshots (`No response`), any second reporter, the
  `confirmed` label. Version 7.4 (i.e. 0.7.4) — old.

## Likely fix
Unknown until the queue-publishing path is instrumented.

## The single blocker
**Not verifiable and not localizable here.** Reproducing needs an Android Auto head unit or DHU;
the environment has no Android SDK at all, let alone AA. Localization would be a guess.

## What would raise the score
A logcat from the AA session, or a maintainer pointing at the queue-publish call site.

## Score
reproduction 15 · diagnostic 3 · localization 0 · blast radius 20 · verifiability 5 = **43/100**

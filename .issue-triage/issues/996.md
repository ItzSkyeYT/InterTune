# #996 — Sync with YT Music doesn't work

**What goes wrong:** A sync playlist made in OuterTune shows up empty on YouTube Music
(both PC and mobile); the sync button doesn't fix it.

**Where it lives:** Not located — this is describing server-side sync behavior with YouTube
Music's backend, not a locally-reproducible logic bug.

**Evidence:** Single report, no logs ("idk how to do that"), no comments, no reactions
visible. Not labelled `confirmed`.

**What a fix would involve:** Unknown — could be an API contract issue with YT Music's
playlist sync endpoint, account-specific state, or a genuine bug in the sync/upload path.
Can't tell without logs.

**Why not fixed this run:** No diagnostic evidence at all (reporter couldn't provide logs),
not reproducible from a code read alone, single unconfirmed report.

**What would raise the score:** A logcat from the reporter showing what request/response
happens during a sync attempt.

**State:** blocked (needs information from reporter — no logs)

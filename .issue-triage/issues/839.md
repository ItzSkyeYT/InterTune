# #839 — Modal bottom sheet does not consume insets

**What goes wrong:** A modal bottom sheet doesn't consume window insets, per two screenshots
attached to the issue (not independently viewable via WebFetch text extraction).

**Where it lives:** Not located. The report gives no component name, no screen name, no
repro steps beyond "see screenshots."

**Evidence:** Labelled `confirmed` by a maintainer, but the report body itself is a joke/troll
submission — "Actual behavior: my life is hard", device/version fields filled with
placeholder text ("AAAAAAAAAA"). No logs, no repro steps, no screenshot content extractable
from this environment (WebFetch cannot render attached images).

**What a fix would involve:** Unknown without seeing the screenshots or getting a repro path
from the reporter — modal bottom sheets appear in several dialogs/screens in this codebase,
so localization would require guessing which one.

**Why not fixed this run:** No diagnostic evidence beyond "confirmed" label; can't name a
file or even a screen with confidence. Localization would score 0 (no citation possible),
which caps total confidence well below the 80 gate regardless of other subscores.

**What would raise the score:** Screenshots need to be viewable (this environment can't
render GitHub-attached images via WebFetch), or the reporter/maintainer needs to name the
specific screen/dialog where the bottom sheet appears.

**State:** blocked (needs information — image content not accessible, no repro steps)

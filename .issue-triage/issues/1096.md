# #1096 — Unsynced playlist (playlists disappear after bot-check prompt)

**What goes wrong:** After logging in and hitting a "confirm you are not a bot" prompt, all
playlists disappear and don't come back even after clearing data/cache.

**Where it lives:** Not located — sounds like a YouTube Music account/auth-flow edge case
(bot-check challenge breaking the session), not a local logic bug.

**Evidence:** Single report, two screenshots (not viewable via WebFetch text extraction), no
logs, not labelled `confirmed`.

**Why not fixed this run:** No diagnostic evidence, likely an auth/session bug tied to
YouTube's bot-check flow rather than a locally reproducible defect; can't localize.

**What would raise the score:** A logcat capturing the auth/sync calls around the bot-check
prompt, and confirmation from a second user.

**State:** blocked (needs information from reporter — no logs)

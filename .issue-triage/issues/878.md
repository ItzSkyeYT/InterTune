# #878 — The app deleted all my downloaded songs

**What goes wrong:** Reporter says all previously downloaded playlists disappeared (0
downloaded tracks shown) after a period of normal use; unclear trigger.

**Where it lives:** Not located — no repro steps, no logs, one-time occurrence reported.

**Evidence:** Single report, no logs, no comments, not labelled `confirmed`. "Is this
normal?" tone suggests reporter isn't even sure it's a bug vs. expected behavior (e.g. a
storage/cache clear, or downloads tied to an account that changed).

**Why not fixed this run:** No diagnostic evidence, not reproducible from a code read alone,
could be caused by several unrelated things (storage permission revocation, cache clear,
external storage unmount, DB reset) with no way to disambiguate without logs.

**What would raise the score:** A logcat from around the time it happened, and confirmation
this isn't storage-permission or external-storage related (mentioned relevant in similar
reports).

**State:** blocked (needs information from reporter — no logs)

# #1075 — Unable to search songs

**What goes wrong:** Reporter says searching "refreshes endlessly" instead of showing
results (title says "unable to search", body text has expected/actual swapped, but the
gist is: search doesn't return results).

**Where it lives:** Not located. Could be the same class of bug as #1190 (see
`.issue-triage/issues/1190.md`) or could be a network/API-layer issue specific to this
reporter's account/region — indistinguishable without logs.

**Evidence:** Single report, no logs, no comments, not labelled `confirmed`.

**Why not fixed this run:** No diagnostic evidence, can't localize with confidence, and
"refreshes endlessly" could be a network issue outside our control.

**What would raise the score:** A logcat or screen recording, and confirmation from a
second user.

**State:** blocked (needs information from reporter — no logs)
